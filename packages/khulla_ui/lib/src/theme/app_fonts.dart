/// Bundled font family names registered in `khulla_ui` `pubspec.yaml`.
abstract final class AppFonts {
  /// [Lexend](https://fonts.google.com/specimen/Lexend) — bundled under
  /// `packages/khulla_ui/assets/fonts/`.
  ///
  /// Package fonts are registered as `packages/<package>/<family>` in the
  /// font manifest, not by bare family name alone.
  static const String lexend = 'packages/khulla_ui/Lexend';
}
